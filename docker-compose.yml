services:
  backend: 
   image: desdecerobackend:0.1
   build:
    context: ./config/django-image
   volumes:
   - ./src/django:/usr/app:delegated
   ports:
   - 8000:8000
   networks:
    red:

networks:
  red:


